# final_fig1_fig2_fixed 修改报告

## 1. 图 1 修复

- 图 1 已重新绘制为高清 PNG。
- 格式：PNG。
- 分辨率：4800 × 1800 px，600 dpi。
- 内容保留五个核心模块：高频 DApp 请求、链下执行层 / Simulated TEE、数据可用性层 / mock-verifiable DA、链上提交层 / compact commit、验证仲裁层 / challenge / recover。
- 保留正常路径和异常路径箭头。
- 已使用更大的文字、加粗边框和加粗箭头。
- 已裁掉原图四周多余空白，使用更紧凑画布。

## 2. 图 2 修复

- 图 2 已重新绘制为高清 PNG。
- 格式：PNG。
- 分辨率：4800 × 1500 px，600 dpi。
- 主流程保留：PENDING → OPEN → RESPONDED → NARROWING → READY → REPLAYED → RESOLVED。
- 超时恢复分支保留：NARROWING → TIMEOUT → RECOVERED → READY。
- 状态框、箭头和关键文字已放大并加粗。
- 已裁掉多余空白，避免使用低清截图。

## 3. 跨双栏与排版

- 只将图 1 和图 2 改为跨双栏显示。
- 通过连续分节符实现：图前恢复双栏节，图所在区域为单栏跨栏，图后恢复双栏正文。
- 图 1 和图 2 均为嵌入型图片，未使用浮于文字上方、紧密型或四周型环绕。
- 图题保留在图片下方，中英文图题均完整保留。
- 图号和正文“如图 1 所示”等引用未改动。
- 其他图片、表格和参考文献未做修改。

## 4. Word 图片压缩设置

- 已在 DOCX 设置中写入 `doNotAutoCompressPictures`。
- 已设置默认图片分辨率为 300 dpi。
- 建议在 Word 中打开后再人工确认一次：文件 → 选项 → 高级 → 图像大小和质量 → 勾选“不压缩文件中的图像”，默认分辨率选择“高保真”。

## 5. PDF 检查

- 已导出 `final_fig1_fig2_fixed.pdf` 并渲染为页面 PNG 检查。
- 图 1 和图 2 均完整显示，未截断、未拉伸、未遮挡。
- 图 1、图 2 前后正文仍为双栏。
- 未发现多余空白页。
- 由于图 1、图 2 改为跨双栏，PDF 页数为 11 页；页数变化来自核心图放大，不是正文压缩或删改。

## 6. 仍需人工检查

- 图 1 和图 2 已按高清位图方式修复；如投稿系统或 Word 版本对 PNG 再压缩，建议最终提交前在 Word 中确认“不压缩文件中的图像”处于启用状态。
