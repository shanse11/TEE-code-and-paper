# -*- coding: utf-8 -*-
"""第三轮：全文术语统一与中文学术风格精修。"""
from docx import Document
from docx.shared import Pt
import re

doc = Document(r"E:\项目\project_code\paper_outputs\draft\final_chinese_academic_format.docx")

def set_text(idx, text):
    doc.paragraphs[idx].text = text

# ============================================================
# Phase 1: Abstract (P4) - Chinese-ify
# ============================================================
set_text(4,
    "高频去中心化应用（Decentralized Application, DApp）如元宇宙状态同步、去中心化社交交互和链上 AI 推理服务，"
    "使区块链扩容问题呈现出典型的安全系统权衡：系统既需要较低交互成本和较低延迟，又不能在异常场景下放弃公开可验证性与挑战活性。"
    "全链上执行具有较强公开验证属性，但难以承受大量小额、连续状态更新；完全链下执行虽然降低成本，却缺少可公开审计的仲裁路径；"
    "单纯依赖可信执行环境（Trusted Execution Environment, TEE）又会引入硬件信任、宿主可用性和远程证明边界。"
    "围绕这一矛盾，本文研究混合 TEE 汇总中一个范围较窄但关键的问题：当执行者仅向链上提交轻量承诺、完整负载数据位于数据可用性层，"
    "而挑战过程又因超时停滞时，协议如何恢复争议、重放证据并完成仲裁结算。"
    "本文的目标并非给出生产级汇总完整实现，而是构造并评估三个相互关联的机制接口：可恢复挑战机制、携带证据的紧凑提交以及数据可用性感知成本模型。"
    "基于 Python 原型、本地以太坊虚拟机燃料实测与 Sepolia 公开测试网部署记录，实验结果显示紧凑提交平均约为 406 字节；"
    "在给定成本模型与数据可用性配置下，紧凑提交配合模块化数据可用性采样相比全链上调用数据在负载 8192、提示长度 64、批大小 1000 时"
    "得到 96.33% 的配置化估算降本；可恢复挑战机制在合成超时场景下将挑战成功率从 0% 提升至 100%。"
    "这些结果用于说明机制接口、活性差异和成本趋势，不应外推为真实 TEE 安全、生产级数据可用性或主网费用结论。"
)

# ============================================================
# Phase 2: Introduction body (P8-P11) - Chinese-ify
# ============================================================
set_text(8,
    "高频去中心化应用的困难并不只是吞吐量不足。此类应用的单次交互价值通常不高，却要求持续状态更新、低延迟反馈、低链上负担，"
    "以及异常场景下的公开可验证仲裁。以链上 AI 推理服务为例，用户连续提交提示，执行者在可信执行环境中生成响应，"
    "并向链上提交紧凑提交；完整提示、响应与证明保存在数据可用性层。"
    "正常路径需要尽量轻量，但异常路径必须具备足够的可验证性：当响应与数据可用性负载不一致，"
    "或挑战在中途超时时，系统不能仅报告"异常已检测"，还需要保证争议能够继续推进到重放与结算。"
)

set_text(9,
    "现有路线分别缓解了上述矛盾的一部分，但难以同时满足低成本、低延迟、公开可验证和挑战活性四项要求。"
    "全链上执行保留了公开验证能力，却将高频负载、存储和执行成本直接推到链上；"
    "完全链下执行降低了延迟和成本，却缺少用户可依赖的公开仲裁路径；"
    "纯 TEE 执行可以降低正常路径的验证成本，但系统安全性和可用性仍受硬件、宿主输入输出、远程证明以及服务连续性的影响。"
    "混合 TEE 汇总的优势在于将 TEE 快速路径与链上异常路径结合起来；"
    "相应地，其关键风险也在异常路径：如果缺少恢复机制和证据入口，系统可能在最需要仲裁时失去协议连续性。"
)

set_text(10,
    "超时后挑战流程卡死并非普通工程异常，而是协议活性与仲裁连续性问题。"
    "传统挑战机制可以关注错误是否能够被发现，但对于高频去中心化应用而言，检测只是争议处理的起点："
    "未完成的挑战会占用状态、延迟结算，并使错误提交停留在不可结算的中间状态。"
    "因此，本文以"故障发生、恢复、重放、仲裁结算"为主线。异常被观察后，协议需要恢复挑战会话；"
    "恢复后，紧凑提交必须能够指向重放所需证据；重放之后，仲裁结果才能进入结算或惩罚。"
)

set_text(11,
    "轻量提交同样存在安全语义边界。若紧凑提交只是普通哈希，虽然能够压缩链上字节，"
    "却无法指明验证者如何取回负载、验证字段级成员证明，或为重放构造证据上下文。"
    "本文将紧凑提交视为最小可验证证据入口：它在正常路径保持链上提交轻量，"
    "在异常路径仍能回指数据可用性负载、Merkle 证明、证明摘要和执行摘要。"
    "实验部分采用合成负载、模拟 TEE、可验证数据可用性注册表、本地以太坊虚拟机燃料实测与 Sepolia 可部署性证据；"
    "这些证据用于评估机制接口与趋势，而非声称生产网络性能。"
)

# ============================================================
# Phase 3: Contributions (P13-P15) - Chinese-ify
# ============================================================
set_text(13,
    "本文的第一个贡献在于提出可恢复挑战机制。在传统挑战流程中，超时可能使争议停留在未解决状态，"
    "即使系统已经检测到异常，也无法自然推进到重放与结算。对于高频去中心化应用而言，"
    "连续结算能力比单次异常检测更为关键。本文将恢复操作从普通的工程恢复提升为保持协议活性的状态转移机制："
    "该操作不修改争议事实、预期执行轨迹或声称执行轨迹，而是在挑战事实不变的条件下将停滞的挑战会话恢复到可重放、可仲裁的路径上，"
    "从而保证了协议的仲裁连续性。"
)

set_text(14,
    "本文的第二个贡献在于设计携带证据的紧凑提交。该提交不是普通哈希摘要，"
    "而是将状态根、输出哈希、证明哈希、数据可用性指针与数据可用性根绑定在同一上下文中，"
    "使链上对象同时具备重放准备性、证据可追溯性和面向验证的属性。"
    "其意义在于：正常路径仅提交最小摘要，异常路径仍可通过数据可用性证据进入验证与重放流程。"
)

set_text(15,
    "本文的第三个贡献在于构建数据可用性感知成本模型。该模型不是价格预测器，"
    "而是用于分析负载缩放、批处理摊销与数据可用性路径选择的轻量化工具。"
    "它帮助系统设计者理解全链上调用数据在何种条件下会成为结构性瓶颈，"
    "批处理摊销在高频工作负载中为何重要，以及具备重放准备性的数据可用性证据会如何改变紧凑提交方案的成本边界。"
)

# ============================================================
# Phase 4: Watchdog terminology - 监督节点 → 观察节点
# ============================================================
for i in range(len(doc.paragraphs)):
    t = doc.paragraphs[i].text
    if '监督节点' in t:
        t = t.replace('监督节点', '观察节点')
        doc.paragraphs[i].text = t

# ============================================================
# Phase 5: Fix Table 3 (invariants) - 仲裁连续性 row
# ============================================================
table = doc.tables[2]
# Row 3 (index 3) = 仲裁连续性 row
cells = table.rows[3].cells
cells[1].text = (
    "在挑战事实已固定、证据可获得且数据可用性证明可检查的可仲裁争议中，"
    "协议应避免超时导致的永久停滞，并使争议继续推进至重放、结算或惩罚阶段"
)
cells[2].text = "恢复操作是保持仲裁连续性的核心机制；将超时停滞会话恢复至可推进状态"

# ============================================================
# Phase 6: Compress Chapter 8 repeated expressions
# ============================================================
# P204 (8 intro): shorten the theoretical claim, remove duplication with 8.5.1
set_text(204,
    "本节将局限性与论文主张一并讨论。本文的价值在于在限定假设下形成一个安全系统论证："
    "可恢复挑战机制改善仲裁连续性，携带证据的紧凑提交保留重放上下文，"
    "数据可用性感知估算刻画高频工作负载下的成本边界。"
    "模拟 TEE、可验证数据可用性注册表、合成负载和本地以太坊虚拟机基线限制了外部效度，"
    "但并不改变本文关注的机制层问题。"
)

# P235 (8.5.1): keep the theoretical contribution statement, trim slightly
set_text(235,
    "从理论层面看，本文的主要价值不在于提出新的汇总系统，而在于将挑战恢复从工程操作提升为具有协议语义的独立机制。"
    "可恢复挑战机制的核心理论贡献是提出并论证了仲裁连续性这一协议性质："
    "恢复操作不是简单的超时重试，而是在保持挑战事实不变的条件下恢复协议活性的状态转移。"
    "本文通过协议性质分析（第 4.4 节）系统化了挑战事实不变性、安全性与活性的分离、仲裁连续性和恢复操作正确性四项性质，"
    "并给出了协议不变量表格与抗滥用分析，为后续将恢复机制推广到更复杂的汇总协议栈提供了理论参照。"
)

# ============================================================
# Phase 7: Additional Chinese-ification in specific paragraphs
# ============================================================

# P23: Rollup background
set_text(23,
    "汇总的基本思想是将大量执行过程移至链下完成，并将状态承诺、证明或争议入口提交到链上。"
    "零知识汇总通过有效性证明保证提交状态的正确性，具有较强的验证属性，但证明生成、验证电路设计和复杂系统实现带来较高成本。"
    "乐观汇总假设链下执行结果默认正确，只有在挑战期内出现有效争议时才对结果进行仲裁。"
    "乐观汇总的优势是实现门槛相对较低、链上负担较轻；其弱点是争议窗口和交互期间的状态不确定性。"
)

# P26: DA background
set_text(26,
    "数据可用性关注链下或链外数据是否能够被需要的验证者获取。"
    "模块化区块链和惰性账本等路线强调将数据可用性从执行验证中解耦，使共识层主要负责数据排序和可用性保证，"
    "而执行和验证由独立的汇总或客户端完成。"
    "当执行者将完整负载放在数据可用性层而非链上时，关键问题在于链上提交如何与数据可用性条目形成可验证的绑定。"
)

# P29: attack surface
set_text(29,
    "攻击面覆盖提交者、执行环境、数据可用性绑定和挑战流程。本文假设执行者或提交者可能提交错误响应、"
    "篡改数据可用性负载、提供无效证明，或在挑战流程中延迟响应；数据可用性层可能出现负载缺失、Merkle 证明损坏或根不一致；"
    "挑战流程本身也可能因交互超时而中断。本文不假设执行者在硬件 TEE 层面受到保护，也不假设数据可用性层具有无限可用性。"
)

# P31: system overview
set_text(31,
    "3 系统概览"
)

set_text(32,
    "图 1 给出 Hybrid TEE 汇总原型的系统架构。系统由执行层、数据可用性接口和验证层三个主要部分构成。"
    "正常路径上，用户在可信执行环境中提交提示并生成响应，执行者生成紧凑提交上链，完整负载写入数据可用性层。"
    "异常路径上，挑战者通过验证层发起争议，系统加载紧凑提交与数据可用性证据，进入交互式争议定位与重放流程；"
    "若流程因超时中断，观察节点可恢复会话并继续推进到仲裁结算。"
)

# P36: execution layer
set_text(36,
    "执行层负责接收用户提示、在模拟 TEE 中完成推理并生成紧凑提交，"
    "将完整负载存入数据可用性层，并将紧凑提交上链。提交内容包含状态根、输出哈希、证明哈希、数据可用性指针和数据可用性根，"
    "从而形成最小可验证证据入口。"
)

# P39: DA interface
set_text(39,
    "数据可用性接口管理负载的存储、可用性状态和 Merkle 证明。"
    "每条负载包含提示、响应、证明和可信执行环境标识，"
    "其字段级内容通过 Merkle 根与链上提交绑定，"
    "使验证者能够按字段验证数据可用性条目的完整性。"
)

# P42: verification layer
set_text(42,
    "验证层负责调用挑战、恢复、重放和结算流程。验证者可通过字段级 Merkle 证明检查数据可用性条目的一致性；"
    "挑战者在发现不一致时打开争议会话；二分缩小阶段定位争议区间；重放阶段比较预期哈希与声明哈希；"
    "结算阶段根据重放结果判定交易状态（最终确认或罚没）。超时后观察节点可恢复会话。"
)

# P64: challenge flow narrative
set_text(64,
    "当交易处于待定状态时，挑战者可以调用打开操作进入已打开状态。"
    "系统加载紧凑提交、数据可用性状态和相关不一致证据。随后，被挑战方进入响应阶段，提供其对争议的回应。"
    "若争议涉及执行轨迹，协议进入二分步骤阶段，通过二分缩小定位争议区间。"
    "对于长度为 n 的轨迹，理想情况下定位轮次接近 log2(n)，从而避免对长执行轨迹进行线性扫描。"
)

# P65: replay
set_text(65,
    "当争议区间被缩小到单步时，协议进入单步重放。重放阶段比较预期哈希与声明哈希，判断局部执行是否一致。"
    "如果重放失败，结算阶段可以将交易标记为罚没状态；如果重放通过，则挑战不成立或进入相应结算逻辑。"
)

# P66: recover
set_text(66,
    "恢复是协议中的关键状态转移，因为它在不改变证据内容的前提下修复流程推进。"
    "在不可恢复的挑战中，超时可能使会话停留在"争议已知但无法重放和结算"的状态。"
    "可恢复挑战机制引入观察节点恢复路径，使超时会话重新回到可进行二分步骤、重放和结算的状态。"
    "这里需要区分活性与正确性：恢复操作恢复的是前者，而后续重放仍负责验证后者。"
)

# P67: three paths
set_text(67,
    "协议流程可分为三条路径。第一是正常路径：若挑战期内没有有效争议，交易最终进入最终确认。"
    "第二是可仲裁异常路径：若响应篡改或轨迹长度不匹配等执行相关故障出现，"
    "协议通过打开、响应、二分步骤、重放与结算完成争议定位和结算。"
    "第三是超时恢复路径：若挑战过程因超时中断，观察节点可以调用恢复将会话推进到已恢复状态，"
    "并继续后续重放与结算。该划分有助于避免将异常检测、活性恢复和经济惩罚混写为同一性质。"
)

# P73: 4.4 intro - fix terms
set_text(73,
    "前述讨论已将可恢复挑战机制的核心性质从"超时后可以重试"这一工程事实"
    "提升为协议层面的状态转移机制。本节进一步将这一认识系统化为挑战事实不变性、"
    "安全性与活性的分离、仲裁连续性和恢复操作正确性四项性质，"
    "并给出协议不变量表格，以明确恢复机制的协议语义边界。"
)

# P103: Chapter 5 intro
set_text(103,
    "本节说明紧凑提交为什么不是单纯压缩结构。其研究价值在于携带异常路径所需的最小证据关联。"
    "该提交绑定状态根、输出哈希、证明哈希、数据可用性指针和数据可用性根："
    "状态根约束输入、输出与环境摘要；输出哈希约束响应；证明哈希约束证明；"
    "数据可用性指针定位完整负载；数据可用性根则承诺字段级证明成员关系。"
)

# P104: DA binding
set_text(104,
    "这种绑定关系使验证者能够从链上提交进入数据可用性证据结构。"
    "若响应被篡改，验证者检查响应叶与 Merkle 根；若证明与输出不一致，证明哈希和负载哈希形成交叉约束；"
    "若数据可用性证明损坏，系统可以将异常定位为证明无效，而不是混同为执行重放失败。"
    "由此，紧凑提交支持后续挑战和重放的证据加载，而不仅服务于链上字节压缩。"
)

# P105: adversarial perspective
set_text(105,
    "从对抗角度看，提交者可能替换负载字段、隐藏数据可用性条目、提交与证明不一致的输出，或在争议过程中延迟响应。"
    "紧凑提交本身并不能解决真实数据可用性网络可用性或真实 TEE 信任问题；"
    "其更窄的贡献在于明确链上最小承诺需要绑定哪些证据字段，才能使后续挑战具备重放准备性，而不仅是节省链上字节。"
)

# P107: cost model intro
set_text(107,
    "成本模型关注全链上与紧凑提交配合数据可用性在高频场景下的结构性差异。"
    "全链上路线将完整负载作为链上数据提交，其成本随负载大小增大近似线性增长。"
    "紧凑提交配合数据可用性将负载发布从链上调用数据转移到数据可用性配置，"
    "链上只保留紧凑提交，从而在高频大批量场景中控制成本增长。"
)

# P108: DA routes
set_text(108,
    "模型比较四类数据可用性路径：全链上调用数据、紧凑外部数据可用性、紧凑类 EIP-4844 方案和紧凑模块化数据可用性采样。"
    "该比较属于配置化估算，应被理解为研究原型的成本建模工具，而非对真实主网定价的模拟。"
)

# P110: batch insight
set_text(110,
    "从系统角度看，批大小越大，固定提交成本越容易被摊薄；负载越大，全链上调用数据的线性成本越突出；"
    "数据可用性路径越接近低边际数据成本，紧凑提交配合数据可用性的收益越明显。"
    "因此，该模型的核心启示不在于某项数字，而在于批处理规模、负载大小和数据可用性路径选择三者之间存在结构性权衡。"
)

# ============================================================
# Phase 8: Fix remaining English in Table 0 and Table 1
# ============================================================
# Table 0: 机制推进
t0 = doc.tables[0]
t0.rows[1].cells[1].text = "在超时或中断后恢复争议流程"
t0.rows[2].cells[1].text = "绑定状态/输出/证明/数据可用性根"
t0.rows[3].cells[1].text = "区分证明、数据可用性、超时、重放和轨迹故障"
t0.rows[4].cells[1].text = "联合负载、批处理和数据可用性路径分析成本"

# Table 1: 机制语义对比 - row 5 (本文工作)
t1 = doc.tables[1]
t1.rows[5].cells[1].text = "可恢复挑战状态机"
t1.rows[5].cells[2].text = "恢复操作作为保持活性的状态转移"
t1.rows[5].cells[3].text = "紧凑提交到数据可用性字段级证明的绑定"
t1.rows[5].cells[4].text = "负载/批处理/路径权衡分析"

# Also fix row 3 (OTR) and row 4 (TEERollup) English
t1.rows[3].cells[1].text = "TEE 辅助的乐观验证"
t1.rows[3].cells[2].text = "超时恢复讨论有限"
t1.rows[3].cells[3].text = "部分负载/证明关联"
t1.rows[3].cells[4].text = "本文未建模"

t1.rows[4].cells[1].text = "TEE 快速路径与挑战入口"
t1.rows[4].cells[2].text = "主要作为系统机制讨论"
t1.rows[4].cells[3].text = "架构层面讨论"
t1.rows[4].cells[4].text = "未给出轻量数据可用性摊销模型"

# ============================================================
# Phase 9: Fix table captions with English mixing
# ============================================================
# Actually these are paragraph captions, let me scan for them
for i, p in enumerate(doc.paragraphs):
    t = p.text.strip()
    # Don't modify figure/table captions that already have Chinese
    if t.startswith('表 ') and ('（' in t and any(c.isascii() and c.isalpha() for c in t[t.index('（'):t.index('）')+1] if '）' in t[t.index('（'):])):
        # Has English parenthetical - acceptable for first mention
        pass

# ============================================================
# Phase 10: Fix remaining scattered English terms  
# ============================================================

# Define replacement map - careful to only replace in narrative text, not code/filenames
replacements = [
    # watchdog fixes (already done above via 观察节点)
    # remaining terms in narrative
    ('challenge session', '挑战会话'),
    ('challenge state machine', '挑战状态机'),
    ('recovery function', '恢复函数'),
    ('bisection narrowing', '二分缩小'),
    ('single-step replay', '单步重放'),
    ('mismatch evidence', '不一致证据'),
    ('payload hash', '负载哈希'),
    ('trace length mismatch', '轨迹长度不匹配'),
    ('response tampering', '响应篡改'),
    ('attestation invalid', '证明无效'),
    ('da unavailable', '数据可用性不可用'),
    ('da proof invalid', '数据可用性证明无效'),
]

# Apply to all paragraphs
for i, p in enumerate(doc.paragraphs):
    t = p.text
    for eng, chn in replacements:
        t = t.replace(eng, chn)
    if t != p.text:
        doc.paragraphs[i].text = t

# ============================================================
# Phase 11: Verify table numbering continuity
# ============================================================
# Already verified in previous round. Just do a quick sanity check.
import re
print("Table references in text:")
for i, p in enumerate(doc.paragraphs):
    refs = re.findall(r'表\s*\d+[\-\w]*', p.text)
    if refs:
        print(f"  P{i}: {refs}")

print()
print("Figure references in text:")
for i, p in enumerate(doc.paragraphs):
    refs = re.findall(r'图\s*\d+', p.text)
    if refs:
        print(f"  P{i}: {refs}")

doc.save(r"E:\项目\project_code\paper_outputs\draft\final_chinese_academic_format.docx")
print()
print("Done. Paper refined and saved.")
