# -*- coding: utf-8 -*-
"""修改论文：添加4.4协议性质分析，强化理论表达。"""
from copy import deepcopy
from docx import Document
from docx.shared import Pt
import os

SRC = r"E:\项目\project_code\paper_outputs\draft\final_chinese_academic_format.docx"
DST = SRC  # Overwrite

doc = Document(SRC)

def mkp(text, style="Normal"):
    p = doc.add_paragraph(text, style=style)
    doc.element.body.remove(p._element)
    return p

# Phase 1: Build all new paragraphs for 4.4
new_items = []

new_items.append(mkp("4.4 协议性质分析", "Heading 2"))

new_items.append(mkp(
    "前面的讨论已经说明，可恢复挑战机制的核心性质并非\"超时后可以重试\"这一工程事实，"
    "而是恢复操作在保持挑战事实不变的前提下恢复了协议活性。本节将这一认识系统化为四项协议性质："
    "挑战事实不变性、安全性、活性（仲裁连续性）和恢复操作正确性，"
    "并给出协议不变量表格与抗滥用分析，以明确恢复机制的协议语义边界。"
))

# (1)
new_items.append(mkp("（一）挑战事实不变性", "Heading 3"))
new_items.append(mkp(
    "挑战会话在被创建时，会将一组不可修改的协议事实绑定在会话上下文中。"
    "这些事实定义了争议的\"内容面\"：争议针对的是哪一次提交、哪一段执行、哪一个负载承诺。"
    "具体而言，挑战事实包括以下不可修改字段："
))
new_items.append(mkp(
    "• 状态根（State Root）：约束输入与输出哈希的绑定关系；"
    "\n• 数据可用性根（DA Merkle Root）：约束 DA 层中完整负载的字段级承诺；"
    "\n• 负载哈希（Payload Hash）：标识 DA 层中完整的原始负载；"
    "\n• 执行轨迹（Execution Trace）：挑战方与被挑战方各自提交的执行步骤序列；"
    "\n• 重放证据（Replay Evidence）：二分缩小后定位到的目标步骤与对应的 expected hash / claimed hash；"
    "\n• 不一致证据（Mismatch Evidence）：在验证、响应和 DA 字段检查中观测到的字段级不一致记录。"
))
new_items.append(mkp(
    "用符号表示，若将挑战会话 cs 的挑战事实集合记为 Facts(cs)，"
    "则恢复操作满足：Facts(cs_before) = Facts(cs_after)。"
    "即恢复操作不会向挑战会话注入新的状态根、新的负载哈希、新的执行轨迹或新的重放证据。"
))
new_items.append(mkp(
    "需要强调的是，恢复操作恢复的是流程推进能力，而不是修改争议内容本身。"
    "在不可恢复的挑战协议中，超时会话会导致会话不可逆地停滞，使得已经观测到的异常无法进入重放和仲裁——"
    "这相当于系统\"看到了问题但无法处理\"。可恢复挑战机制的核心设计意图，正是将检测与处理之间的鸿沟打通，"
    "同时保证处理过程不能脱离原始检测到的事实基础。"
))

# (2) 安全性
new_items.append(mkp("（二）安全性分析：恢复机制不会削弱协议安全", "Heading 3"))
new_items.append(mkp(
    "一个自然的问题是：恢复操作是否可能被恶意利用，从而降低协议的安全保障？本小节分析此问题。"
))
new_items.append(mkp(
    "恢复操作不会接受错误执行结果。恢复后的重放阶段仍然对 expected hash 与 claimed hash 进行逐位比较，"
    "两者不一致时挑战成功。恢复仅改变了\"谁来推进流程\"，未改变\"什么算正确\"的判断标准。"
    "\n\n恢复操作不会修改重放正确性。重放阶段的验证依赖于二分缩小定位到的目标步骤，"
    "该步骤在恢复前后保持不变。恢复不能将失败的 expected hash 替换为通过的 expected hash。"
    "\n\n恢复操作不会篡改挑战证据。不一致证据、DA 证明状态和字段级不一致记录在恢复前后不变。"
    "恢复后的 DA 字段验证将基于相同的 DA Merkle 根进行。"
    "\n\n恢复操作不会重写负载承诺。紧凑提交中的 DA Merkle 根和 DA 指针在提交时已锁定，"
    "恢复操作无法将指针重新指向一个新的 DA 条目或修改负载哈希。"
))
new_items.append(mkp(
    "总结构成安全性的核心判断：恢复机制恢复的是协议活性，而不是重新定义正确性。"
    "正确性的最终裁决仍由重放阶段完成，恢复只是保证该裁决环节能够被执行，"
    "而非替代裁决本身。"
))

# (3) 活性分析
new_items.append(mkp("（三）活性分析：仲裁连续性", "Heading 3"))
new_items.append(mkp(
    "仲裁连续性（Arbitration Continuity）是本文提出的用于刻画挑战协议活性性质的概念。"
    "其定义如下：如果一个挑战协议能够保证\"所有已经检测到的争议，"
    "最终都能够进入重放、仲裁或惩罚阶段\"，则认为该协议具备仲裁连续性。"
))
new_items.append(mkp(
    "传统基于超时的挑战协议可以完成异常检测：验证者观测到 DA 不一致、响应不匹配或证明无效后，"
    "能够将异常记录为争议。然而，传统方案不能保证仲裁流程继续推进——"
    "当挑战方或被挑战方在交互过程中超时未响应，或挑战会话因链上资源竞争而无法及时推进时，"
    "争议可能永久停留在中间状态，交易既不能最终确认也不能被罚没。"
    "\n\n这就是检测性与完成性之间的差异：传统方案聚焦于\"能否发现异常\"，"
    "而本文关注的仲裁连续性聚焦于\"已发现的异常能否被处理完毕\"。"
    "对于高频去中心化应用，这一差异具有实际意义——"
    "未完成的挑战会话会占用状态、延迟结算，使链上对象长期处于不确定状态，"
    "这与高频交互所需的高结算确定性直接矛盾。"
))
new_items.append(mkp(
    "可恢复挑战机制通过恢复函数在超时后恢复仲裁流程推进能力："
    "监管者或运营者调用恢复功能将使超时会话回到可推进状态（已响应或缩小区间），"
    "从而继续二分缩小、重放和结算。"
    "实验结果表明，在 100 个样本中，恢复后的挑战成功率从 0% 提升至 100%，"
    "验证了恢复机制将超时场景从\"停滞\"转变为\"可继续推进\"的能力，"
    "即协议在超时场景下保持了仲裁连续性。"
))

# (4) 恢复操作正确性
new_items.append(mkp("（四）恢复操作正确性：恢复活性与保持正确性的分离", "Heading 3"))
new_items.append(mkp(
    "恢复操作正确性的核心在\"分离\"二字：将影响协议活性的状态与影响协议正确性的状态分开管理。"
))
new_items.append(mkp(
    "恢复操作允许修改的状态仅限于活性相关字段："
    "\n• 超时状态：将会话的超时计时器重置，使会话从超时停滞状态回到可交互窗口；"
    "\n• 会话推进状态：将会话状态标记从停滞状态恢复到先前状态（已响应或缩小区间），"
    "恢复后的会话可以继续接收二分步骤调用；"
    "\n• 交互窗口状态：恢复后的会话获得新的超时窗口，使挑战方与被挑战方能够继续交互。"
))
new_items.append(mkp(
    "恢复操作不允许修改的状态包括："
    "\n• 重放证据：expected hash、claimed hash 和目标步骤在恢复前后保持不变；"
    "\n• 执行轨迹承诺：二分缩小过程中积累的区间信息不会被重置或重写；"
    "\n• 负载绑定：DA 指针与 DA Merkle 根在恢复前后保持不变，"
    "恢复操作不能将挑战指向另一个 DA 条目；"
    "\n• 数据可用性证明：Merkle 证明路径和 DA 可用性状态不受恢复操作影响。"
))
new_items.append(mkp(
    "因此，恢复操作无法将错误执行伪装为正确执行。"
    "恢复操作所做的是：当检测到流程因超时而停滞时，在不改写争议内容的前提下使流程重新获得推进能力。"
    "这正是\"恢复活性\"与\"保持正确性\"之间的分离设计："
    "活性层保证流程不被超时永久阻塞，正确性层保证裁决基于原始事实。"
    "这种分离设计的协议意义在于，系统在对抗场景下获得了一种能力——"
    "即使恶意节点试图通过拖延交互来耗尽挑战窗口，"
    "系统仍可在窗口之外恢复流程并将争议推进至仲裁结算。"
))

# Protocol invariants table
new_items.append(mkp("协议不变量与保持性质", "Heading 3"))
new_items.append(mkp(
    "表 2-A 汇总了本文协议设计所依赖的核心不变量及其在恢复操作下的保持性质。"
    "这些不变量是连接\"恢复是安全的\"与\"恢复是有效的\"两个主张的协议层桥梁。"
))

table = doc.add_table(rows=6, cols=3)
table.style = "Table Grid"
doc.element.body.remove(table._element)

headers = ["协议不变量", "含义", "在恢复操作下的保持性质"]
rows_data = [
    ["挑战事实完整性", "状态根、DA 根、负载哈希、执行轨迹、重放证据和不一致证据在会话生命周期内不可修改", "恢复操作不改变任何挑战事实字段"],
    ["重放正确性", "重放阶段的 expected hash 与 claimed hash 比较是裁决正确性的唯一依据", "恢复操作不修改 expected hash 或 claimed hash；不改变目标步骤"],
    ["仲裁连续性", "所有已检测到的争议最终都能进入重放、仲裁或惩罚阶段", "恢复操作是保持仲裁连续性的核心机制；将超时停滞会话恢复至可推进状态"],
    ["数据可用性绑定", "紧凑提交中的 DA Merkle 根约束 DA 条目的字段级内容", "恢复操作不修改 DA 指针或 DA Merkle 根"],
    ["执行轨迹一致性", "二分缩小过程中积累的区间信息是重放定位的依据", "恢复操作不重置或重写区间信息"],
]
for j, h in enumerate(headers):
    cell = table.cell(0, j)
    cell.text = h
    for p in cell.paragraphs:
        for r in p.runs:
            r.bold = True
            r.font.size = Pt(10)
for i, rd in enumerate(rows_data):
    for j, val in enumerate(rd):
        cell = table.cell(i + 1, j)
        cell.text = val
        for p in cell.paragraphs:
            for r in p.runs:
                r.font.size = Pt(10)
new_items.append(table)

new_items.append(mkp(
    "上述不变量共同构成可恢复挑战协议的协议安全边界："
    "恢复操作被限制在活性状态空间内，不能跨越到正确性状态空间。"
    "这种边界设计使恢复机制的安全论证不依赖于具体实现（Python 原型或 Solidity 合约），"
    "而只依赖于挑战事实与活性状态的分离。"
))

# Anti-abuse analysis
new_items.append(mkp("恢复机制的抗滥用分析", "Heading 3"))
new_items.append(mkp(
    "一个需要考虑的对抗场景是：恶意节点是否可能通过滥用恢复机制来破坏协议的正确性或公平性？"
    "本节分析几种典型的滥用尝试，说明恢复机制在协议层面为何具有抗滥用性质。"
))
new_items.append(mkp(
    "情形一：恶意节点试图通过调用恢复来重写挑战证据。"
    "恢复操作不会修改不一致证据、DA 证明状态或字段级不一致记录。"
    "恢复将超时会话标记为已恢复时，证据集合保持不变。"
    "后续的重放和结算仍然基于原始证据进行裁决。因此，恶意节点无法通过恢复来\"清洗\"已经记录的证据。"
))
new_items.append(mkp(
    "情形二：恶意节点试图在恢复后注入新的执行轨迹。"
    "恢复操作不会修改 expected trace 或 claimed trace。"
    "二分缩小过程中积累的区间边界和中间状态在恢复前后保持一致。"
    "恢复后的二分步骤调用基于已有的区间信息继续推进，不能重新定义争议区间。"
))
new_items.append(mkp(
    "情形三：恶意节点试图通过反复超时和恢复来发起资源耗尽攻击。"
    "恢复操作需要在链上执行（Solidity 实现中恢复挑战消耗约 156,891 gas），"
    "每次调用需要支付 gas 成本。此外，恢复操作由监管者或运营者发起，"
    "不是任意账户的无限制操作。在更高安全要求的部署中，"
    "可以对恢复次数或恢复间隔设置上限以限制滥用窗口。"
    "但需要指出，当前研究原型尚未实现恢复频率限制机制，这一方向留给后续工作。"
))
new_items.append(mkp(
    "情形四：恶意节点试图通过恢复绕过挑战正确性。"
    "恢复操作不改变重放阶段的判断逻辑。"
    "重放阶段对 expected hash 和 claimed hash 的比较是确定性的："
    "如果两者不一致，挑战成功；如果一致，挑战失败。"
    "恢复操作不修改这个比较过程，也不修改参与比较的两个哈希值。"
    "因此，即使恶意节点获得了恢复操作的控制权，也无法将不一致的重放结果变为一致。"
))
new_items.append(mkp(
    "综合以上分析，恢复机制之所以具有抗滥用性质，根本原因在于："
    "恢复操作只能作用于已经链上承诺的挑战会话，而这些会话的挑战事实在创建时即已锁定。"
    "恢复操作恢复了流程活性，但不会为恶意节点提供修改协议正确性判断依据的途径。"
    "这一性质使恢复机制区别于\"重新开始挑战\"（可能丢失已建立的证据关联），"
    "也区别于\"替换执行轨迹\"（会破坏仲裁的公平性）。"
))

# Insert all items before P72
p72_el = doc.paragraphs[72]._element
for item in new_items:
    if hasattr(item, '_element'):
        el = item._element
    else:
        el = item._element
    p72_el.addprevious(el)

# Phase 2: Modify P13 (contributions)
doc.paragraphs[13].text = (
    "本文的第一个贡献在于提出可恢复挑战机制（Recoverable Challenge）。"
    "在传统挑战流程中，超时可能使争议停留在未解决状态，即使系统已经检测到异常，"
    "也无法自然推进到重放与结算。对于高频去中心化应用而言，"
    "连续结算能力比单次异常检测更为关键。本文将恢复操作从工程恢复提升为保持协议活性的状态转移机制："
    "该操作不修改争议事实、预期执行轨迹或声称执行轨迹，"
    "而是在挑战事实不变的条件下将停滞的挑战会话恢复到可重放、可仲裁的路径上，"
    "从而保证了协议的仲裁连续性（Arbitration Continuity）。"
)

# Phase 3: Modify section 4.3 paragraphs
doc.paragraphs[69].text = (
    "性质一，数据可用性绑定性质。紧凑提交中的 DA Merkle 根与 DA 条目中的字段级 Merkle 证明之间建立了密码学绑定关系，"
    "因此提示词、响应或证明字段被替换后，会导致负载哈希、叶哈希或 Merkle 证明与链上根不一致。"
    "该性质不依赖真实 DA 网络，只说明当前可验证 DA 结构能够检测字段篡改，并保证紧凑提交与完整负载之间在证据层面不可脱钩。"
)
doc.paragraphs[70].text = (
    "性质二，恢复机制的仲裁连续性。恢复操作的目标是恢复超时挑战会话的流程推进能力，"
    "使其继续进入二分缩小、重放和结算；它不修改预期执行轨迹、声称执行轨迹或重放不一致的事实。"
    "因此，恢复改变的是挑战流程的活性连续性，而非争议正确性本身。"
    "该性质将恢复语义与安全裁决语义明确分开，避免将\"流程可以继续\"与\"裁决结果改变\"这两个不同层次的性质混淆。"
)
doc.paragraphs[71].text = (
    "性质三，二分定位性质。对于长度为 n 的执行轨迹，二分挑战步骤每轮缩小争议区间，理想轮次接近 log2(n)。"
    "本文实验中轨迹步数为 4、8、16、32、64、128 时，平均二分轮次分别为 2、3、4、5、6、7，"
    "与理论对数趋势一致。该性质说明争议定位的开销增长是亚线性的，"
    "使得执行轨迹较长时挑战的交互成本仍可接受。"
)

# Phase 4: Modify 8.5.1
doc.paragraphs[205].text = (
    "从理论层面看，本文更有价值的部分不是提出新的 Rollup 系统，而是将挑战恢复从工程补丁提升为具有协议语义的独立机制。"
    "可恢复挑战机制的核心理论贡献在于提出并论证了仲裁连续性这一协议性质："
    "恢复操作不是简单的\"超时重试\"，而是在保持挑战事实不变的条件下恢复协议活性的状态转移。"
    "携带证据的紧凑提交与 DA 感知成本模型的组合，"
    "则为混合可信执行环境汇总在高频去中心化应用场景下提供了一条\"正常路径轻量、异常路径可验证\"的理论设计路线。"
    "本文通过协议性质分析（第 4.4 节）系统化了挑战事实不变性、安全性与活性的分离、仲裁连续性和恢复操作正确性四项性质，"
    "并给出了协议不变量表格与抗滥用分析，为后续将恢复机制推广到更复杂的汇总协议栈提供了理论参照。"
)

doc.save(DST)
print("Done. Modified paper saved.")
