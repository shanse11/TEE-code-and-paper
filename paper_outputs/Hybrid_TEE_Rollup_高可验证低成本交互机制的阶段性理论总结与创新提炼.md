# Hybrid TEE-Rollup 高可验证低成本交互机制的阶段性理论总结与创新提炼

## 1. 研究背景与问题定义

当前区块链系统在面向高频交互类应用时面临一个持续存在的结构性问题：一方面，应用希望获得接近 Web2 系统的低延迟、高吞吐与低单次交互成本；另一方面，区块链系统又必须维持公开可验证、去中心化安全和可追责的执行边界。对于元宇宙状态同步、去中心化社交关系更新、链上 AI 推理请求等场景而言，这种矛盾尤为突出，因为其业务特征往往表现为“大量、小额、连续、状态敏感”的交互流，而不是少量高价值交易。

在这一背景下，单纯依赖全链上执行会带来明显的性能与成本瓶颈；而完全转向链下执行又会削弱公开可验证性，甚至使执行过程沦为外部黑箱。因此，当前研究关注的问题并不是抽象意义上的“如何再提出一个新的 Rollup”，而是如何在已有 Hybrid TEE-Rollup 路线下，构造一种既能压低高频交互成本、又能在异常条件下保持可验证与可仲裁性的机制原型。

基于当前代码、实验和部署基础，本文档所总结的研究问题可以收敛为：

> 在已有 Hybrid TEE-Rollup 思路下，如何面向高频 DApp 场景，构造一种兼顾低成本、可验证与异常可恢复性的交互机制，并重点研究 recoverable challenge、compact commit + verifiable DA 以及其在链上实现路径中的阶段性可行性。

这一问题定义决定了当前工作的研究定位：它是一项以机制设计、系统逻辑和阶段性实验支撑为核心的研究原型工作，而不是面向生产环境的完整基础设施实现。

## 2. 高频 DApp 场景中的核心矛盾

高频 DApp 的系统需求与传统链上执行模型之间存在三类核心矛盾。

### 2.1 全链上执行与成本约束之间的矛盾

高频 DApp 的交互往往具有较强的状态连续性，但单次请求本身的经济价值未必足以支撑完整链上执行和全量数据存储的成本。如果将每次状态更新、社交交互、推理请求或结果同步都作为独立链上执行事务处理，则链上计算、存储与数据可用性成本会迅速累积。这意味着系统虽然保持了较强的公开可验证性，却牺牲了高频场景最敏感的成本效率与交互速度。

### 2.2 全链下执行与公开可验证性之间的矛盾

与此相对，如果系统将绝大部分执行外包给链下节点或中心化服务，则吞吐与成本压力确实会下降，但新的问题随之出现：执行结果由谁担保？若执行者篡改响应、隐藏状态、延迟应答或在争议阶段失联，外部观察者是否仍然能够恢复验证路径？在这一意义上，全链下执行虽然缓解了性能瓶颈，却削弱了区块链系统最核心的公共可信边界。

### 2.3 高吞吐目标与去中心化安全性之间的矛盾

高频 DApp 并不只是要求更高的 TPS，而是要求系统在更高交互密度下仍保有以下能力：  
其一，能够对链下执行结果进行最小但足够的链上留痕；其二，能够在出现异常时启动仲裁；其三，仲裁本身不能因为 timeout、数据缺失或状态分歧而彻底停滞。由此可见，问题的核心并非“是否做扩容”，而是“是否能在低成本执行条件下维持异常时的恢复性与可验证性”。

基于上述矛盾，Hybrid TEE-Rollup 的必要性体现为：它提供了一条介于全链上和全链下之间的中间路线，即通过可信链下执行、轻量链上提交、可验证外部数据和交互式挑战机制，构造一种面向高频 DApp 的“可验证低成本交互”结构。当前工作正是在这一总体路线之下，进一步聚焦其尚未被充分讨论的部分：**异常情况下争议流程的恢复性与持续推进能力**。

## 3. 当前研究主线与问题收敛

当前工作的主线已经不再是笼统地“构建一个 Hybrid TEE-Rollup 系统”，而是收敛为一个更明确、更适合当前阶段代码与实验支撑的研究切口：

1. **Recoverable Challenge**  
   交互式挑战不应仅停留在“发现错误”的层面，还应在 timeout、中断、轨迹不一致、数据不可用等异常场景下具备恢复推进的能力。

2. **Compact Commit + Verifiable DA**  
   轻量提交不只是压缩链上字节，而应作为验证路径的最小证据入口，与 DA 根、payload hash、Merkle proof 和 challenge evidence 建立明确关联。

3. **面向高频 DApp 的可验证低成本交互分析**  
   当前研究讨论的性能不是单纯 TPS，而是一个同时考虑成本摊销、可验证性、异常恢复和 DA 路线差异的统一分析对象。

因此，当前工作并不宣称提出了一个新的 Rollup 范式，而是在已有 Hybrid TEE-Rollup 思路下，推进了如下研究问题：

> 当链下执行已由 TEE 路线提供初步可信性时，如何通过轻量链上提交、可验证 DA 结构和可恢复挑战状态机，使系统在高频交互条件下既获得成本收益，又在异常场景中保留可仲裁性与协议活性。

这一收敛使得论文主线具备更清晰的学术边界：研究重点不是重新发明基础设施，而是在既有方向上推进“异常情况下仍可完成仲裁”的机制能力。

## 4. 系统整体设计逻辑

当前系统已经形成由执行层、数据层和验证层构成的三层闭环结构，其总体逻辑可以概括为：**链下执行、链上留痕、异常时可恢复验证**。

### 4.1 执行层

执行层负责承载高频交互请求的实际计算过程。当前原型采用 deterministic mock model 或可选 Ollama 后端完成链下执行，并以 simulated TEE attestation 形式对输入哈希、输出哈希、nonce 和执行环境标识进行绑定。其理论目标不是证明真实硬件 TEE 的最终安全性，而是构造“可信链下执行摘要”这一最小原型单元。

从系统逻辑看，执行层承担两项任务：

1. 将高成本、连续、高频的请求处理尽可能放到链下完成；
2. 输出足够精炼但可验证的执行摘要，以供后续链上提交和挑战流程使用。

因此，执行层解决的是“谁来做计算”和“如何为链下计算结果留下可验证入口”的问题。

### 4.2 数据层

数据层的关键设计在于，完整 payload 不再全量上链，而是被拆分为两类对象：

- 链上保留的 `compact commit`
- DA 层保存的完整 payload 与证明结构

当前 commit 中包含：

- `state_root`
- `output_hash`
- `proof_hash`
- `da_pointer`
- `da_merkle_root`

DA 侧则进一步组织：

- `payload_hash`
- `leaf_hashes`
- `merkle_root`
- `merkle_proofs`

这种结构的意义在于，链上对象不再只是一个孤立的哈希入口，而是成为面向后续 challenge 的最小证据骨架。换言之，数据层并非简单追求“把东西放便宜一点的地方”，而是在构造一条从链上紧凑提交到链下完整数据、再到字段级 Merkle proof 的连续验证路径。

从理论上看，数据层的目标是将“低成本提交”与“可验证数据可用性”绑定起来，使轻量提交既能够降低链上成本，又不会切断异常时的证据回溯路径。

### 4.3 验证层

验证层是当前系统最核心的研究部分。它已经实现了从 `challenge-open` 到 `challenge-resolve` 的完整交互式争议路径，并扩展了以下关键能力：

- timeout recovery
- bisection narrowing
- single-step replay
- replay arbitration
- failure injection and fault observation

其中，bisection 负责逐步缩小争议范围，single-step replay 负责对局部可疑执行进行细粒度重放，而 recover 机制则负责在超时或中断场景下恢复挑战会话，使其能够继续推进。

从系统逻辑上看，验证层不再只是“最后出事了再看一下”，而是承担两个关键目标：

1. 将异常从不可追踪的失败事件转化为可观测、可分类、可进入挑战流程的状态；
2. 将挑战从一次性的 happy-path 仲裁推进为一个在异常条件下仍具有 liveness 的状态机。

由此，整个系统形成了一个完整闭环：执行层负责高频交互计算，数据层负责构造低成本但可验证的链上留痕与外部数据结构，验证层负责在异常时启动、恢复并完成仲裁。

## 5. 当前系统已经完成的关键能力

基于当前代码、实验脚本与部署结果，系统已经不再停留在概念设计阶段，而具备了若干可明确陈述的关键能力。

第一，系统已经完成了 **执行层、数据层与验证层的原型闭环**。这意味着当前工作并非局部模块实验，而是具备了提交、记录、验证、恢复和仲裁的连续流程。

第二，系统已经具备了 **多类异常场景的故障注入与观测能力**。当前原型已覆盖 `response tampered`、`attestation invalid`、`trace length mismatch`、`DA unavailable`、`DA proof invalid`、`challenge timeout without recover`、`challenge timeout with recover` 等多类失败路径，这为后续 fault taxonomy 的理论提炼提供了直接基础。

第三，系统已经具备了 **批量实验框架**。当前正式实验已扩展为：

- 成本实验记录数：`3200`
- 挑战实验记录数：`600`
- 失败场景实验记录数：`800`
- 每组变量样本数：`100`

这说明实验已经从组会 smoke run 过渡到可支撑论文 Evaluation 的阶段性规模。

第四，系统已经具备了 **Solidity 链上映射能力**。当前原型中的关键路径已映射为：

- `MockTEEVerifier`
- `MockDARegistry`
- `HybridTEERollup`
- `MerkleVerifier`

第五，系统已经具备了 **本地 EVM `gasUsed` 实测基线**。关键链上操作例如 `submit rollup`、`challenge open`、`challenge recover`、`challenge replay`、`challenge resolve` 等均已在 Hardhat 本地测试链中获得真实 `gasUsed` 结果，这为 Python 原型中的结构性成本趋势提供了链上对应物。

第六，系统已经具备了 **公开测试网部署证据**。当前 Solidity 合约已成功部署到 Sepolia，并生成真实测试网地址、部署区块与 ABI 索引。这说明当前研究已经形成从 Python 原型到本地 EVM 再到公开测试网的渐进式验证路径。

这些能力共同表明：当前工作已经是一项具有机制、原型、实验和链上部署支撑的研究方向，而不是停留在抽象设想阶段的方案讨论。

## 6. 理论创新与机制推进（核心部分）

### 6.1 Recoverable Challenge

在已有 optimistic verification 或 TEE-Rollup 相关工作中，challenge 机制往往主要被视为一种错误检测与争议定位工具，即系统重点关注“是否能发现错误”以及“是否能在理想路径下完成仲裁”。当前工作的推进在于，将 challenge 的研究重点从单纯的错误检测扩展到**异常场景下可恢复地完成仲裁**。

这一推进的理论意义体现在三个方面：

1. **从安全性扩展到活性（liveness）**  
   challenge 不再只是保证错误结果最终可被指出，还需要保证在 timeout、中断或数据缺失时，争议流程不会永久停滞。

2. **从一次性事务扩展为容错状态机**  
   当前系统中的 challenge 已经不再是简单请求—应答结构，而是包含 `open / respond / step / recover / replay / resolve` 的状态推进过程，因此可以被建模为 fault-tolerant challenge state machine。

3. **从“异常被发现”推进到“异常被处理完”**  
   在无 recover 的情况下，系统可能仅停留在检测到问题但流程中断；而 recover 机制使系统能够恢复会话并继续推进至 replay 与 slashing。

因此，Recoverable Challenge 的核心创新不在于增加一个额外接口，而在于将 challenge 从“错误可检测”推进为“异常下仍可完成仲裁”的协议能力。

### 6.2 Evidence-Carrying Compact Commit

当前工作中的 compact commit 已不再是简单的压缩提交摘要，而是逐步演化为一种**证据携带型轻量提交（evidence-carrying compact commit）**。

这一结构的核心特征在于：

- commit 中包含 `da_merkle_root`
- DA 对象中包含 `payload_hash`
- 字段级证明由 `merkle_proof` 支撑
- challenge session 中存在明确的 `evidence linkage`

由此，链上 commit 的角色发生了变化：它不再只是“为了省链上空间而压缩一次”，而是成为后续异常验证与 replay 仲裁的证据入口。换言之，当前系统中的 compact commit 同时承担了两种职责：

1. 作为低成本的链上摘要结构；
2. 作为可恢复 challenge 启动时的最小证据骨架。

其理论价值在于，它将“低成本提交”和“证据可追溯性”统一起来，从而避免系统陷入“越便宜越难验证”的结构性困境。

### 6.3 Fault Taxonomy

在很多原型系统中，异常往往被统一表示为 “invalid execution” 或“恶意节点行为”。当前工作的一个重要推进，是将异常细化为更明确的故障类型学（fault taxonomy），包括：

- attestation fault
- DA fault
- timeout fault
- replay fault
- trace inconsistency

在当前原型与实验中，这一分类具体体现为：

- `attestation_invalid`
- `da_unavailable`
- `da_proof_invalid`
- `challenge_timeout_no_recover`
- `challenge_timeout_recover`
- `response_tampered`
- `trace_length_mismatch`

这一细化具有两层理论意义：

第一，它使异常分析从“单一恶意行为模型”推进到“多维度故障模型”；  
第二，它为系统分层提供了更明确的责任边界，即某些故障主要由执行层暴露，某些故障主要由数据层暴露，某些故障则只有在验证层交互中才会显现。

因此，fault taxonomy 不只是列举更多异常，而是为后续论文中的安全分析和机制职责划分提供更清晰的理论框架。

### 6.4 DA-aware Cost Amortization

当前工作的成本分析不再停留于“数据是否上链”这一单一判断，而是扩展为一个同时受以下因素影响的结构性问题：

- DA 路线差异
- payload size
- batch size
- prompt length

实验中已经显式比较：

- `full-onchain`
- `compact + external DA`
- `compact + EIP-4844-like`
- `compact + modular DA`

其理论推进在于：成本收益被建模为一个与数据大小、批处理规模和 DA 价格路线共同相关的趋势问题，而不是一个固定比例。

这意味着，当前系统讨论的“低成本”不是静态结论，而是一个 DA-aware cost amortization 框架。在这一框架下，论文可以更清楚地说明：

1. compact commit 的链上固定开销相对稳定；
2. payload 增大后，full-onchain 路线成本近似线性增长；
3. batch size 增大后，单笔摊销成本显著下降；
4. 不同 DA 路线决定了降本收益的上界与下界。

### 6.5 高频 DApp 的可验证性能分析

当前工作的一个重要视角是，将“高频 DApp 的可验证性能”作为统一分析对象。这里的性能不是单纯 TPS，而是同时考虑以下几个维度：

- cost amortization
- recoverability
- verifiability
- DA-aware interaction

这一定义方式的意义在于，它将扩容问题从单纯追求吞吐量转向更加贴近应用场景的系统分析：高频 DApp 真正需要的不是抽象的 TPS 最大化，而是在持续交互条件下维持合理成本、可验证性与异常可恢复性。

因此，当前工作并非仅从底层基础设施视角出发，而是在构造一种更符合高频 DApp 需求的“可验证低成本交互”分析范式。

### 6.6 渐进式链上验证路径

当前工作已形成一条较为清晰的渐进式验证路径：

> Python 原型 -> 本地 EVM gas 实测 -> Sepolia 部署

这一路径的价值不在于简单展示“能部署”，而在于为研究原型建立分层证据链：

1. **Python 原型** 用于快速验证机制逻辑和结构性趋势；
2. **本地 EVM 实测** 用于获得关键链上操作的真实 `gasUsed` 基线；
3. **Sepolia 部署** 用于证明系统已具有公开测试网对应物和最小链上可部署性。

这一渐进式路径使当前研究从“纸面机制”推进为“原型—链上对应物—公开测试网证据”的连续过程，也使论文能够更克制但更有力地说明当前工作的工程外延。

## 7. 实验结果支持的阶段性理论结论

当前实验结果并非仅用于展示原型是否跑通，而是在支撑若干明确的理论主张。

第一，**compact commit 具有明显的固定成本特征**。实验中，compact commit 在不同 payload 下基本保持稳定，而 full payload 随数据规模近似线性增长。这支持一个重要判断：当前提交结构的链上成本主要与摘要结构相关，而不是与完整业务负载等比例绑定。

第二，**payload 增大后，轻量提交的收益显著扩大**。在较大 payload 场景下，相比 full-onchain 路线，`compact commit + DA` 的降本比例达到较高水平。这说明高频、大量、连续交互的应用越接近“大数据但低单次价值”的特征，越能从 compact commit 结构中获得收益。

第三，**batch size 增大后，amortized gas 显著下降**。这一现象支撑了当前系统面向高频 DApp 的应用合理性，因为高频交互场景的自然优势之一就在于更容易通过批处理摊薄链上成本。

第四，**recoverable challenge 明显改善了协议活性**。超时场景对照结果表明，无 recover 时系统虽然能检测异常，但 challenge 无法完成；引入 recover 后，challenge success 与 slashed 比例显著改善。这说明 recover 机制在理论上并不是附属功能，而是支撑 challenge liveness 的关键组成部分。

第五，**bisection replay 的轮次增长与 `log2(trace steps)` 基本一致**。这说明当前争议定位并非暴力线性扫描，而是保留了二分查找式复杂度结构。因此，recover 机制的引入并未破坏 challenge 定位阶段的基本效率逻辑。

第六，**异常场景实验表明系统已具备较完整的故障可观测性与部分完整仲裁路径**。特别是在 `response_tampered` 和 `trace_length_mismatch` 等场景下，系统能够完成检测、challenge 和 slashing，说明 fault taxonomy 已经不只是理论分类，而有了相应实验支撑。

第七，**本地 EVM 实测与 Sepolia 部署为理论主张提供了链上外延证据**。这使当前研究不再只是“趋势估算”，而开始具备“关键路径已在链上获得对应物”的证据基础。

因此，当前实验与部署结果共同支撑了如下阶段性理论判断：

> 当前原型已经能够从机制上支撑一种面向高频 DApp 的、兼顾低成本、可验证性与异常可恢复性的交互路径；其中，recoverable challenge 提供异常场景下的仲裁活性，compact commit + verifiable DA 提供结构性降本基础，而本地 EVM 与 Sepolia 部署则提供了链上对应物的阶段性证明。

## 8. 当前工作的局限性

尽管当前研究已经具备机制、原型、实验与部署支撑，但仍需明确其边界，以避免不当外推。

第一，**TEE 仍为 simulated TEE**。当前 attestation 和 verifier 主要用于验证机制路径与接口逻辑，而不代表真实硬件 TEE 的完整安全边界。

第二，**DA 仍为 mock DA 原型**。当前系统实现了 Merkle proof、DA root 和可用性状态记录，但尚未接入真实生产级 DA 网络，因此相关结论更适合作为“可验证数据组织方式”的原型性验证。

第三，**Python prototype 主要用于结构性趋势验证**。其成本分析虽有参考价值，但其中相当一部分仍来自 gas 估算模型，而非真实公网交易成本。

第四，**本地 EVM 不等于主网环境**。Hardhat 本地链的 `gasUsed` 结果为链上关键操作提供了重要基线，但仍不能直接视为主网或公开测试网的最终费用。

第五，**Sepolia 部署仅代表阶段性公开测试网落地**。虽然当前已成功完成合约部署，但 Sepolia 不等同于生产系统场景，且 Etherscan 源码验证与逐操作链上回执整理仍待补充。

第六，**当前系统仍是研究原型而非生产系统**。其定位是支撑机制分析、系统设计和阶段论文写作，而不是直接面向真实用户负载或开放网络部署。

这些局限并不削弱当前工作的研究价值，恰恰相反，它们有助于更清晰地界定当前结论的适用范围：**当前结论主要用于证明机制合理性、趋势有效性和原型可行性，而非主网级性能定论。**

## 9. 当前阶段的理论总结

如果从研究路线的角度对当前阶段进行概括，可以将其归纳为以下三层逻辑。

第一，**问题层面**，当前工作已经明确指出高频 DApp 的核心困难不是简单的吞吐不足，而是低成本执行、公开可验证性与异常可恢复性之间的结构性冲突。

第二，**机制层面**，当前工作已经在 Hybrid TEE-Rollup 路线下形成了一套相对清晰的三层逻辑：执行层通过 simulated TEE 提供链下可信执行摘要，数据层通过 compact commit + verifiable DA 保留低成本证据入口，验证层通过 recoverable challenge 和 replay arbitration 在异常场景下维持仲裁活性。

第三，**证据层面**，当前工作已经形成由 Python 原型、正式实验、本地 EVM `gasUsed` 基线和 Sepolia 部署共同构成的渐进式验证路径。这使得理论主张不再仅依赖口头描述，而有了机制、原型、实验和链上部署的连续支撑。

因此，当前阶段最合适的理论性总结可以表述为：

> 本研究并未试图重新发明 Rollup，而是在已有 Hybrid TEE-Rollup 思路下，围绕高频 DApp 的可验证低成本交互问题，推进了 recoverable challenge、evidence-carrying compact commit 和 fault taxonomy 等关键机制，并通过实验与链上部署证据表明，该方向已经形成一个具有明确论文写作价值的阶段性研究原型。

## 10. 后续研究方向

在当前基础上，后续研究最值得沿以下几个方向继续推进。

第一，继续扩大正式实验规模，并补充更多 `payload size`、`batch size`、`DA profile` 和 `trace steps` 组合，使结构性趋势在更大样本下得到更稳健的统计支撑。

第二，将 Python 侧的 gas 估算模型与本地 EVM 实测结果进一步对齐，形成更明确的“趋势估算—链上基线”映射关系。

第三，继续完善 fault taxonomy，将当前异常类型进一步组织为更正式的分层安全模型，并区分“可直接验证型故障”“需交互仲裁型故障”“需恢复后仲裁型故障”等不同类别。

第四，重试 Sepolia 合约源码验证，并增加更多公开测试网交易样本，以增强部署层证据的完整性。

第五，在论文层面将当前机制总结进一步形式化，包括：

- Recoverable Challenge 的状态机描述
- Evidence-Carrying Compact Commit 的结构化定义
- DA-aware Cost Amortization 的参数化表达
- 高频 DApp 可验证性能的统一分析框架

第六，进一步与相关路线进行对照分析，例如 Optimistic Rollup、ZK Rollup、State Channel 以及其他 Hybrid TEE-Rollup/OTR 类工作，从而更清晰地界定当前工作的适用边界与差异化价值。

总体而言，后续工作的重点不应是“做更大的系统”，而应是继续沿着已经形成的研究主线，把当前阶段原型上升为更正式、更清晰、更具论文表达力的机制性研究成果。

